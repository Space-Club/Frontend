import { whiteGreyBox } from '@/styles/common';
import styled from '@emotion/styled';

const ClubMemberContainer = styled(whiteGreyBox)`
  width: 100%;
  height: 100%;
  display: flex;
  flex-wrap: wrap;
  overflow: auto;
`;

export { ClubMemberContainer };
